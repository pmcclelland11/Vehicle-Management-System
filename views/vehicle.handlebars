     {{>nav}}
    
    
    <main>
        
        
       
        
        <section> 
            <input id="filter-input"> 
            <button id="filter-button">
                Search
            </button>
        </section>
        <table class="table table-striped"> 
            <tr>
                <th>ID</th>
                <th>Make</th>
                <th>Model</th>
                <th>Year</th>
                <th>Mileage</th>
                <th>Type</th>
                <th>Price</th>
                <th>Status</th>
            </tr>
            {{#each vehicles}}
            
                <a href="/vehicle/{{id}}">
            <tr class="tableRow">
                <td>{{id}}</td>
                <td>{{make}}</td>
                <td>{{model}}</td>
                <td>{{year}}</td>
                <td>{{mileage}} miles</td>
                <td>{{type}}</td>
                <td>${{price}}</td>
                <td>{{#if isSold}} Sold {{else}} Available {{/if}}</td>
            </tr>
            </a>
            {{/each}}
           
        </table>
    </main>
    <script src="./js/vechicle.js"></script>
{{!-- 
    {{> nav}}

<main>
    <section>
        <input id="filter-input">
        <button id="filter-button">Search</button>
    </section>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Make</th>
                <th>Model</th>
                <th>Year</th>
                <th>Mileage</th>
                <th>Type</th>
                <th>Price</th>
                <th>Status</th>
                <th>Action</th> <!-- Add a column for Edit and Delete buttons -->
            </tr>
        </thead>
        <tbody>
            {{#each vehicles}}
            <tr class="tableRow">
                <td>{{id}}</td>
                <td>{{make}}</td>
                <td>{{model}}</td>
                <td>{{year}}</td>
                <td>{{mileage}} miles</td>
                <td>{{type}}</td>
                <td>${{price}}</td>
                <td>{{#if isSold}} Sold {{else}} Available {{/if}}</td>
                <td>
                    <button class="btn btn-primary edit-button" data-vehicle-id="{{id}}">Edit</button>
                    <button class="btn btn-danger delete-button" data-vehicle-id="{{id}}">Delete</button>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</main>

<!-- Modal for Editing -->
<div class="modal" id="editModal">
    <!-- Add your edit form here -->
</div>

<!-- Modal for Confirming Deletion -->
<div class="modal" id="deleteModal">
    <div class="modal-content">
        <p>Are you sure you want to delete this vehicle?</p>
        <button class="btn btn-danger" id="confirmDelete">Delete</button>
        <button class="btn btn-secondary" id="cancelDelete">Cancel</button>
    </div>
</div>
<script src="./js/vehicle.js"></script> --}}
